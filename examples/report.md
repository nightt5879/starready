# Example StarReady Report

Run this command to generate a fresh report for the current repository:

```bash
node bin/starready.js . --markdown STARREADY_REPORT.md
```

The generated report includes:

- Overall score and grade.
- Top priority fixes with point impact.
- Category score table.
- Per-check evidence for CI review.
